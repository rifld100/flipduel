name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run build -w @flipduel/shared
      - run: npm run test -w @flipduel/shared
      - run: npm run build -w @flipduel/api
      - run: npm run build -w @flipduel/web
